Mahdi Monazami
96440263
oslab5

1.
پس از اجرای این دستور از شما یوزرنیم و پسورد میخواهد و پس از وارد کردن از دفعات بعد دیگه نیازی به این کار نیست

git config -- global credential.helper store


2.

.gitignore:

این فایل برای نادیده گرفتن بررسی بعضی از فایل ها و یا فولدر های پروژه توسط گیت استفاده میشود تغییرات آن ها را نادیده میگیرد و انها را دنبال نمیکند

.git:

با ایجاد گیت در پروژه مون این فایل هاید ایچاد میشود که در آن تمام تغییرات پروژه ذخیره و تمام اطلاعات سیو میشود

git pull:

برای بروزرسانی پروژه ازش استفاده میشود و آخرین تغییرات رو از سرور میگیرد

branch:

اطلاعات همه شاخه هایی گه در ریپازیتوری فعلی وجود دارد در اختیار ما میگذارد ولی در حالت کلی گیت بصورت پیشفرض یک شاخه اصلی میسازد گه قابل تغییر و ویرایش است

3.

اول یک دایرکتورری ایجاد میکنیم و بعد دستورات زیر را تو پوشه مورد نظرمون ایجاد میکنیم

git init

git add - all

git commit -m "test commit"

git branch -M main

git remote add origin "test URL"

git push -u

4.

با استفاده از دستور 

system-analyze

میتوانیم به اطلاعات مورد نظر دسترسی داسشته باشیم و همچنین از دستور

uptime

هم میتوان برای بدست اوردن تایم روشن بودن سیستم استفاده کرد
